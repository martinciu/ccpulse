# build output
/ccpulse
/bin/
/dist/
/completions/

# editor + os
.DS_Store
*.swp

# planning artefacts (carried across worktrees, never committed)
.superpowers/
.autonomo/
.claude/worktrees/

# personal/machine-local Claude instructions (alongside the committed CLAUDE.md)
CLAUDE.local.md

# skills (installed via `npx skills`, personal tooling)
.agents/
.claude/skills/
skills-lock.json

# coverage
coverage.out
coverage.html

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
