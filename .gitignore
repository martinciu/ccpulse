# build output
/ccpulse
/bin/
/dist/
/completions/

# demo recording fixtures (throwaway; demo/ccpulse.tape + demo/ccpulse.gif are committed)
demo/.cache-dev/
demo/.projects/

# editor + os
.DS_Store
*.swp

# planning artefacts (carried across worktrees, never committed)
.superpowers/
.autonomo/
.claude/worktrees/

# personal/machine-local Claude instructions (alongside the committed CLAUDE.md)
CLAUDE.local.md

# skills (installed via `npx skills`, personal tooling)
.agents/
.claude/skills/
skills-lock.json

# coverage
coverage.out
coverage.html

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
